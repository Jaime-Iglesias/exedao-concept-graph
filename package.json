{
  "name": "exedao-graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ jaime-iglesias/exeDAO",
    "create-local": "graph create --node http://localhost:8020/ jaime-iglesias/exeDAO",
    "remove-local": "graph remove --node http://localhost:8020/ jaime-iglesias/exeDAO",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 jaime-iglesias/exeDAO"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.15.0",
    "@graphprotocol/graph-ts": "^0.15.0"
  }
}
